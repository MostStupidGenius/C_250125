#include <iostream>
#include <string>

using namespace std;

// 추상 클래스와 순수 가상 함수
// 순수 가상 함수란, 상속을 할 때 자식 클래스에서 반드시
// 구현해야 쓸 수 있는 함수를 명시적으로 가리키기 위해
// 선언부만 정의해두고 바디(구현부)를 작성하지 않는 함수

// 순수 가상 함수가 하나라도 포함된 클래스는
// 반드시 추상 클래스로 선언해야 한다.
// 추상 클래스는 순수 가상 함수 포함 여부와 무관하게
// 객체화가 불가능하다.
// -> 순수 가상 함수를 구현을 하지 않으면 객체화를 할 수 없는데
// 순수 가상 함수를 포함할 가능성이 있기 때문에
// 객체화를 할 수 없다.

int main()
{
    



}
